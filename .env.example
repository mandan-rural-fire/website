# Sanity project identifiers. These are PUBLIC values (also hardcoded in
# sanity.config.ts for the Studio); they are env vars so the app code has one
# source of truth. Copy this file to .env for local development, and set both
# in the Netlify UI for deploys.
PUBLIC_SANITY_PROJECT_ID=ri0z6y8l
PUBLIC_SANITY_DATASET=production
